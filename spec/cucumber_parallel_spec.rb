require "spec_helper"

describe CucumberParallel do
  it "has a version number" do
    expect(CucumberParallel::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
